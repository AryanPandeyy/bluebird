/* "use client";
 * import { SWRConfig } from "swr";
 * import { request } from "graphql-request"; */
/* https://eduardowronscki.hashnode.dev/nextjs-fetching-data-with-graphql-request-and-swr */
/* export const SWRProvider = ({ children }) => {
 *   return (
 *     <SWRConfig
 *       value={{
 *         fetcher: (query: string, variables) =>
 *           request("http://localhost:4000/graphql", query),
 *       }}
 *     >
 *       {children}
 *     </SWRConfig>
 *   );
 * }; */
